# backend/routes/__init__.py
# I created this file to mark the routes directory as a Python sub-package.
# Each module I wrote inside here (entities, routes, search) registers its own Blueprint,
# which I then import and attach to the Flask app in backend/app.py.
