# backend/__init__.py
# I created this file to mark the backend directory as a Python package.
# Without it, imports like `from backend.app import create_app` would not resolve.
# I intentionally keep this file empty — all application logic lives in app.py and its sub-modules.
